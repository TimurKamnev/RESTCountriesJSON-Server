export interface DetailCountries {
  id: string;
  name: {
    common: string;
  };
  flags: {
    png: string;
  };
  population: string;
  region: string;
  capital: string;
  status: string;
  subregion: string;
  languages: string;
}